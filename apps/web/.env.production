# Frontend and API are served from the same Cloud Run container.
# Leave these empty so the app uses relative URLs (e.g. /api/docs/upload).
# Vite will replace undefined env vars with "" via the ?? "" fallback in the code.
VITE_API_URL=
VITE_WS_URL=
