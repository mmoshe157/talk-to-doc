# Gemini API key — used for Live sessions, RAG search, and file uploads
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# Server
PORT=3001
NODE_ENV=development

# CORS origins for the web app (comma-separated, or * for all)
CORS_ORIGINS=http://localhost:5173
